<div class="w-full">
    <div class="flex items-center">
        <h1 class="font-bold text-4xl m-auto mt-20"><%= @user.name %>'s Podcasts</h1>
    </div>
    
    <%= render partial: "podcasts/podcasts", locals: {podcasts: @podcasts, sharing: true } %>
</div>