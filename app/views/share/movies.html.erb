<div class="w-full">
    <div class="flex items-center">
        <h1 class="font-bold text-4xl m-auto mt-20"><%= @user.name %>'s Movies</h1>
    </div>
    
    <%= render partial: "movies/movies", locals: {movies: @movies, sharing: true } %>
</div>