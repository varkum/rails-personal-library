<div class="w-full">
    <div class="flex items-center">
        <h1 class="font-bold text-4xl m-auto mt-20"><%= @user.name %>'s Books</h1>
    </div>
    
    <%= render partial: "books/books", locals: {books: @books, sharing: true } %>
</div>