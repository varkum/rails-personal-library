<div class="mx-auto md:w-2/3 w-full">
  
    <% if notice.present? %>
      <p class="py-2 px-3 bg-green-50 mb-5 text-green-500 font-medium rounded-lg inline-block" id="notice"><%= notice %></p>
    <% end %>

    <%= render "form", tvshow: @tvshow, viewing: true, source: "show" %>

  <div class="flex justify-between">
  <div>
    <%= link_to 'Edit', edit_tvshow_path(@tvshow), class: "mt-2 rounded-lg py-3 px-5 bg-gray-100 inline-block font-medium hover:opacity-70" %>
    <div class="inline-block ml-2">
      <%= button_to 'Delete', tvshow_path(@tvshow), method: :delete, class: "bg-red-500 mt-2 rounded-lg py-3 px-5 bg-gray-100 font-medium hover:opacity-70" %>
    </div>
    </div>
    <%= link_to 'Back to TV Shows', tvshows_path, class: "my-auto ml-2 rounded-lg py-3 px-5 bg-gray-100 inline-block font-medium hover:opacity-70" %>
 </div>
</div>
